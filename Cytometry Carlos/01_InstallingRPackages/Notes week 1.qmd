---
title: "Untitled"
format: html
---

Functions from week 1 lessons
```{r}
install.packages("package") #for installing packages from CRAN
install("package") #for installing packages from Bioconductor (requires "BiocManager" from CRAN)
install_github("user/package") #for installing packages from GitHub (requires "remotes" from CRAN)
sessionInfo() #to see which packages are attached/loaded
installed.packages() # to see which packages are installed
```